create database login;

use login;

CREATE TABLE client(pnum int primary key auto_increment, id char(10), pw char(10));

select * from client;
desc client;

show databases;

-- create DATABASE login;

use login;
